# SECURITY WARNING: don't run with the debug turned on in production!
DEBUG=True

# SECURITY WARNING: keep the secret key used in production secret!
DJANGO_SECRET_KEY=AMESecR3TkeY

# ALLOWED_HOSTS separated with commas
ALLOWED_HOSTS=localhost,127.0.0.1

# Project metadata (exposed via the OpenAPI schema)
PROJECT_VERSION=0.1.0
PROJECT_DESCRIPTION=acronyme

# PostgreSQL — uncomment all five to use Postgres; otherwise SQLite is used.
#DB_NAME=acronyme
#DB_USER=acronyme
#DB_PASSWORD=acronyme
#DB_HOST=localhost
#DB_PORT=5432
